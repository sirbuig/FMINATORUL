@model FMInatorul.Models.Student

@{
    ViewData["Title"] = "Edit Year";
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

<h2>Edit Year</h2>

@using (Html.BeginForm(controllerName: "Students", actionName: "EditYear", method: FormMethod.Post,
     routeValues: new { id = Model.Id }))
{
    @Html.LabelFor(m => m.Year, "Anul de studiu")
    <br />
    @Html.EditorFor(m => m.Year, new { htmlAttributes = new { @class = "form-control" } })
    @Html.ValidationMessageFor(m => m.Year, null, new { @class = "text-danger" })

    @Html.HiddenFor(m => m.Semester, new { Value = Model.Semester })
    @Html.HiddenFor(m => m.ApplicationUserId, new { Value = Model.ApplicationUserId })
    @Html.HiddenFor(m => m.ApplicationUser, new { Value = Model.ApplicationUser })
    @Html.HiddenFor(m => m.FacultateID, new { Value = Model.FacultateID })
    @Html.HiddenFor(m => m.Facultate, new { Value = Model.Facultate })
    <br />
    <br />
    <button type="submit" class="btn btn-primary" >Save</button>
}

